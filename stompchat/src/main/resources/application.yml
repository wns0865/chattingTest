spring:
  application:
    name: stompchat
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/backend?serverTimezone=Asia/Seoul
    username: root
    password: wnsgud0815!
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 110MB
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

secrer-key: S3Crt3Key